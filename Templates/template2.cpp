#include <bits/stdc++.h>
using namespace std;

void solve() {
    // Write your code for each test case here
}

int main() {
    // Fast I/O
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t = 1;
    cin >> t; // Remove if there is only one test case
    while (t--) {
        solve();
    }

    return 0;
}
